# Checklist - 审稿逻辑修复 + 字数控制强化

## Task 1: _review_error_result() 修复
- [x] `_review_error_result()` 返回字典包含 `"review_error": True`（第171行）
- [x] `_review_error_result()` 返回字典包含 `"retry_hint"` 字段（第172行）
- [x] write_and_review() 兼容读取：`result.get('issue') or result.get('retry_hint')`
- [x] 审稿超时时返回的 issue 值能正确显示在日志中
- [x] JSON解析失败时返回的 issue 值为 "审稿结果格式异常，无法解析"

## Task 2: 审稿异常重试机制
- [x] write_and_review() 中定义了 MAX_REVIEW_RETRIES = 3（第538-539行）
- [x] review_retry_count 计数器在循环外初始化为 0（第540行）
- [x] review_error 分支中 review_retry_count += 1 后检查 ≤ 3 时 continue
- [x] review_retry_count > 3 时打印"降级为不通过处理"并 pass 到不通过分支
- [x] 正常审稿双重通过后 review_retry_count 重置为 0（第657行）
- [x] 审稿重试日志格式: `[重试审稿] 责任编辑返回异常（xxx），第N/3次重试...`

## Task 3: 字数警告分级
- [x] 硬截断阈值 hard_limit = word_max * 1.2 = 4800
- [x] 超过硬截断时按段落边界（\n\n）裁剪，保留完整段落
- [x] 裁剪后打印 `[裁剪] 字数XXXX超过硬上限4800，已裁剪至YYYY字`
- [x] 字数 ≤ 4400 (≤10%) → 🟢 略超: +X%，+Y字
- [x] 字数 4400-5200 (10-30%) → 🟡 标超: +X%，+Y字 | 建议检查
- [x] 字数 > 5200 (>30%) → 🔴 严重超标: +X%，+Y字 | 建议重写/裁剪
- [x] 不再出现"可接受范围"措辞

## Task 4: max_tokens 收紧 + 硬截断
- [x] 大模型模式 max_tokens = min(int(4000 * 1.2), 4800) = **4800**（第1002行）
- [x] 分段模式 max_tokens = min(int(2000 * 1.5), 4096) = **3000**（第1022,1037行）
- [x] 硬截断逻辑在字数统计之后、保存之前执行
- [x] 裁剪不会破坏章节结构（按 \n\n 段落边界切割）

## 集成测试
- [ ] 重启程序后模拟审稿格式异常 → 应显示 `[重试审稿] 第1/3次...` 而非 `第2次写作尝试`
- [ ] 连续3次审稿异常 → 应显示 `[错误] 审稿连续3次异常，降级为不通过处理`
- [ ] 生成一章检查字数是否在 2800-4500 范围内
- [ ] 故意生成超长文本（>5000字）→ 应触发 🔴 严重超标警告 + 自动裁剪

## 验收标准总结
✅ **根因验证**: _review_error_result() 包含 review_error 标记 ✅
✅ **重试机制**: 审稿异常最多重试3次后才降级重写 ✅
✅ **字段兼容**: issue/retry_hint 双字段兼容读取 ✅
✅ **字数警告**: 三级分级（🟢略超/🟡标超/🔴严重超标）✅
✅ **硬截断**: 超4800字自动按段落裁剪 ✅
✅ **参数收紧**: max_tokens 从5600降到4800，系数从1.4降到1.2 ✅
✅ **向后兼容**: 正常审稿流程不受任何影响 ✅
