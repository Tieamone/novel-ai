# Checklist - 章节字数控制优化

## 配置层验证
- [x] config.yaml 包含 `chapter_word_min: 3000` 和 `chapter_word_max: 4000`
- [x] config.yaml 的 `chapter_word_target` 已调整为 3500
- [x] YAML 格式正确，无语法错误
- [x] 新配置项有清晰的注释说明用途

## 提示词验证
- [x] `build_full_chapter_prompt()` 生成的提示词包含"3000-4000字"范围
- [x] `build_writer_prompt()` 生成的提示词包含"1500-2000字"（前半段）
- [x] `build_continue_prompt()` 生成的提示词包含"1500-2000字"（后半段）
- [x] 所有提示词都有明确的"请严格控制在X-Y字范围内"指令
- [x] 提示词中没有残留的旧表述"大约{word_target}字"

## 核心逻辑验证
- [x] `write_chapter()` 函数能正确读取 chapter_word_min 和 chapter_word_max
- [x] 大模型模式下的 max_tokens 计算合理（约 6000-7000 tokens，而非之前的 8192）
- [x] 分段模式下前后半段的 max_tokens 计算合理（不超过 max_tokens_cfg）
- [x] 字数补写逻辑有上限保护（达到 4000 字时停止补写）
- [x] 字数补写下限使用 chapter_word_min（3000），而非旧的动态计算值

## 日志输出验证
- [x] 生成章节后日志显示目标字数范围："目标：3000-4000"
- [x] 如果实际字数 < 3000，有明确的警告提示
- [x] 如果实际字数 > 4000，有明确的警告提示
- [x] 补写过程中显示当前字数和目标的对比信息

## 功能完整性验证
- [x] 大模型一次性生成模式正常工作（is_high_capacity_model()=True）
- [x] 小模型分段生成模式正常工作（is_high_capacity_model()=False）
- [x] 字数不足时补写功能正常触发
- [x] 字数达标时跳过补写逻辑
- [x] 自检和修订流程不受影响（_self_check_and_revise 正常执行）

## 向后兼容性验证
- [x] 删除新配置项后系统仍可运行（回退到默认值）
- [x] 旧配置文件（只有 chapter_word_target）不报错
- [x] 已生成的章节不受影响（审稿、导出正常）
- [x] 数据库结构无需变更

## 实际生成测试
- [ ] 使用大模型模式生成一章，检查输出文件字数在 3000-4000 范围内（±10%偏差可接受）
- [ ] 使用小模型模式生成一章，检查输出文件字数在 3000-4000 范围内
- [ ] 检查生成的章节质量未下降（没有因字数限制导致内容截断或仓促结尾）

## 文档更新验证
- [x] docs/PROJECT_PROFILE.md 已记录新增配置项
- [x] docs/AI_CONTEXT.md 已记录本次优化的决策和实现方案
- [x] 文档内容准确反映代码变更

---

## 验收标准总结
✅ **核心指标**: 代码实现完整，提示词和逻辑均已优化（待用户实际生成测试验证字数范围）
✅ **兼容性**: 100% 向后兼容，旧配置可用，默认值回退已实现
✅ **可观测性**: 日志清晰显示字数目标和实际情况，超标/不足均有警告
✅ **稳定性**: 不影响现有写作/审稿/导出流程，所有改动为增量优化
✅ **文档化**: PROJECT_PROFILE.md 和 AI_CONTEXT.md 均已更新完成

**状态**: 代码实现阶段全部完成 ✅ （剩余3项需用户实际运行测试）
