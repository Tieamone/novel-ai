# Checklist - 作者风格增强 + 全书任务卡 + 文本导入 + 状态修复

## P0: 状态修复验证
- [x] planner.py 第595行使用 `'待处理'` 而非 `'pending'`
- [x] planner.py 第658行使用 `'待处理'` 而非 `'pending'`
- [x] 运行系统后终端显示"待处理"而非"pending"
- [x] 数据库 chapter_tasks 表新插入记录的状态为中文

## P1: 知名作者风格验证
- [x] AUTHOR_STYLES 包含选项 "7"（刘慈欣）
- [x] AUTHOR_STYLES 包含选项 "8"（金庸）
- [x] AUTHOR_STYLES 包含选项 "9"（古龙）
- [x] 每个风格有完整的 system_prompt（>200字）
- [x] 风格选择界面能显示新增的3个选项
- [x] 选择后写作时使用对应的 system_prompt

## P1: 全书任务卡验证
- [x] split_outline_to_tasks() 支持 full_batch 参数
- [x] full_batch=True 时生成目标章数的全部任务卡
- [x] full_batch=False 时保持原有分批行为（向后兼容）
- [x] 大批量（>100章）时 max_tokens 自动提升至 8000
- [x] run_planner() 询问用户选择分批/一次性
- [x] 一次性生成的任务卡数量等于目标章数
- [x] 超长输出时自动二次补充（容错机制）

## P2: 文本导入功能验证
- [x] newbook_template.txt 模板文件存在于根目录
- [x] _import_from_text_file() 能正确读取文件
- [x] 能解析出：书名、类型、目标章数、大纲、角色、世界观
- [x] 缺少时间线或核心冲突时主动询问用户
- [x] 解析结果在导入前展示给用户确认
- [x] 用户确认后自动完成：数据库创建 → 信息保存 → 任务卡生成
- [x] 导入后直接进入章节写作菜单

## 集成测试
- [x] 完整流程测试：文本导入 → 150章任务卡 → 写作一章 → 状态显示中文
- [x] 向后兼容：原有的交互式创建流程不受影响
- [x] 错误处理：newbook.txt 不存在时有友好提示
- [x] 错误处理：文件格式错误时不崩溃，提示检查格式

---

## 验收标准总结

✅ **P0 状态修复**: 终端100%显示中文状态，无英文残留
✅ **P1 作者风格**: 新增3种知名作者风格（刘慈欣/金庸/古龙），可选可用
✅ **P1 全书任务卡**: 支持一次性生成，也保留分批模式，带自动补充机制
✅ **P2 文本导入**: 从 newbook.txt 一键导入并就绪，支持缺失项主动询问
✅ **向后兼容**: 所有原有功能不受影响
✅ **错误处理**: 异常场景有友好提示和模板引导

**状态**: 全部完成 ✅ 生产就绪
