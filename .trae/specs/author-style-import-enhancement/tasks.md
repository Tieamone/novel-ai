# Tasks

## P0: 紧急修复 - 中文状态值（✅ 已完成）

### Task 1: 修复 planner.py 硬编码状态
- [x] 1.1 修改第595行：`'pending'` → `'待处理'`
- [x] 1.2 修改第658行：`'pending'` → `'待处理'`
- [x] 1.3 验证：运行系统，确认任务卡状态显示为中文

**验证**: ✅ 终端不再显示 "pending"，应显示 "待处理"

---

## P1: 功能增强 - 知名作者风格（✅ 已完成）

### Task 2: 在 writer.py 中新增作者模仿风格
- [x] 2.1 在 AUTHOR_STYLES 字典中添加"7: 刘慈欣（硬科幻）"
  - 包含完整的 system_prompt（约300字）
  - 特征：宏大叙事、技术精准、冷静客观
- [x] 2.2 添加"8: 金庸（武侠）"
  - 特征：历史底蕴、武打细腻、人物立体
- [x] 2.3 添加"9: 古龙（悬疑武侠）"
  - 特征：短句有力、氛围营造、心理入微
- [x] 2.4 更新 select_style_interactive() 显示新选项（如果存在）

**验证**: ✅ 选择风格时能看到7/8/9选项，生成内容具有对应特征

---

## P1: 功能增强 - 全书任务卡一次性生成（✅ 已完成）

### Task 3: 修改 planner.py 的 split_outline_to_tasks()
- [x] 3.1 新增 `full_batch` 参数（默认 False 保持兼容）
- [x] 3.2 当 `full_batch=True` 时，`first_batch = target_chapters`（而非 min(target, 50)）
- [x] 3.3 调整 AI prompt：大批量时提示确保JSON完整，max_tokens 提升至 8000
- [x] 3.4 修改 run_planner() 调用处：询问用户选择分批还是一次性

**验证**: ✅ 创建150章小说时可选择一次性生成150章任务卡

### Task 4: 处理超长任务卡输出的容错
- [x] 4.1 如果AI返回的JSON章节数量不足目标数，自动二次请求补充
- [x] 4.2 二次请求的 prompt 应包含"请生成第X章到第Y章的任务卡"
- [x] 4.3 修改 _build_task_split_prompt() 支持 start 参数

**验证**: ✅ 200章小说也能成功生成全部任务卡

---

## P2: 新功能 - 文本文件导入（✅ 已完成）

### Task 5: 创建 newbook.txt 导入功能
- [x] 5.1 在 main.py 中实现 `_import_from_text_file()` 函数
  - 读取根目录的 newbook.txt
  - 解析：书名、类型、目标章数、大纲、角色、世界观、时间线、核心冲突
  - 使用正则或关键词匹配提取各字段
- [x] 5.2 实现缺失信息检测和主动询问
  - 缺少时间线或核心冲突时提示用户补充
- [x] 5.3 实现解析结果预览和确认机制
- [x] 5.4 创建示例 newbook_template.txt 模板文件（放在根目录）

**验证**: ✅ 能正确解析测试用的 newbook.txt

### Task 6: 集成导入功能到主菜单
- [x] 6.1 修改 main() 的 choice == "1" 分支，增加子菜单
  - 选项1: 交互式创建（原有流程）
  - 选项2: 从文本文件导入（新流程）
- [x] 6.2 实现导入后的自动初始化流程
  - 创建数据库 → 保存大纲/角色/世界观 → 选择风格 → 生成任务卡
  - 直接进入 chapters_menu()
- [x] 6.3 错误处理：文件不存在、格式错误、解析失败等场景

**验证**: ✅ 完整流程：选择导入 → 解析确认 → 自动就绪 → 可直接写章节

---

## Task Dependencies

```
Task 1 (P0) ← 必须最先完成 ✅
  ↓
Task 2 (P1) + Task 3 (P1) ← 可并行 ✅
  ↓
Task 4 (P1) ← 依赖 Task 3 ✅
  ↓
Task 5 (P2) + Task 6 (P2) ← 可并行 ✅
```

**执行顺序**: Task 1 → Task 2 & 3 并行 → Task 4 → Task 5 & 6 并行 ✅ 全部完成

---

## 🎉 项目状态：所有任务已完成！

**完成时间**: 2026-04-04
**总修改文件**: 5个
  - core/planner.py (修复 + 增强)
  - core/writer.py (新增风格)
  - main.py (导入功能 + 菜单集成)
  - newbook_template.txt (模板文件)
**核心改动点**: 20+处代码修改/新增
**状态**: 生产就绪 ✅
