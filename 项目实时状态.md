# 项目实时状态

**更新时间：2026-03-31 00:00**

说明：本文档只记录当前代码已经实现、正在表现出的真实状态。目标是让任何人只看这份文档，就能快速了解项目目前"实际上是什么样"，而不是"理想上应该是什么样"。

---

## 1. 项目当前定位

- 这是一个本地命令行 AI 网文写作系统
- 当前入口文件：`main.py`
- 支持两条主流程：新建小说、继续写作已有小说
- 内置5个AI模型选项（硬编码）：
  - `qwen-plus`（推荐，质量好）
  - `qwen-turbo`（速度快，字数少）
  - `qwen-max`（质量最高，消耗快）
  - `qwen-long`（超长上下文）
  - `gemini-2.0-flash`（免费，需科学上网）

---

## 2. 启动流程详解

### 2.1 新建小说流程

1. 输入小说名称（64字符限制，禁用Windows保留名和非法字符）
2. 输入小说类型/关键词
3. 初始化数据库
4. 写入 `novel_info` 表（id=1, name, genre, status='active'）
5. 确定大纲（手动输入或AI生成）
6. 保存大纲到 `data/{novel_name}/outline.md`
7. 提取角色名单（从大纲提取或手动输入）
8. 生成世界观（参考大纲+角色，≤500字）
9. 保存世界观
10. 生成人物档案（JSON格式，包含外貌/性格/秘密/弱点/位置/状态/关系）
11. 逐个保存角色档案
12. 选择写作风格（6个内置或自定义）
13. 预拆分任务卡（默认50章）
14. 进入章节菜单

### 2.2 继续写作流程

1. 读取 `paths.data_dir`（默认值：data）
2. 扫描其中带 `novel.db` 的目录
3. 读取 `novel_info` 表的类型（genre）
4. 若无 `novel_info` 则显示 `类型未记录`（兼容旧库）
5. 用户选择小说后进入章节菜单

### 2.3 章节菜单功能

| 操作 | 说明 |
|------|------|
| 自动生成下一章 | 写作→自检→审稿→(失败重写,最多3次)→保存+摘要 |
| 批量自动生成 | 按数量循环生成多章 |
| 导出所有已审核章节 | 导出status为'approved'/'force_approved'的章节为TXT |
| 查看完整章节列表 | 显示所有章节及其状态 |
| 查看章节任务卡 | 显示待写章节的plot_goal和emotion_tag |
| 更换写作风格 | 重新选择或自定义风格 |
| 查看详细费用统计 | 显示本次会话的token统计和预估费用 |

---

## 3. 核心模块说明

### 3.1 main.py - 主程序

**职责：**
- 主菜单（新建/继续/删除小说）
- 已有小说列表展示
- 章节菜单交互
- 进度统计展示（已审核/待审/总字数/最近章节/伏笔数/任务卡数）
- 费用统计展示

**关键功能：**
- 删除小说采用双重确认（输入小说名+输入YES）
- 进度展示包含最近10个已审核章节的评分窗口

### 3.2 core/api_client.py - API调用

**职责：**
- 模型选择管理
- DashScope/Gemini 路由
- 会话级费用统计
- 失败重试（指数退避，最多3次）

**模型定价：**
```
qwen-turbo:    输入¥0.0003/千token，输出¥0.0006/千token
qwen-plus:     输入¥0.0008/千token，输出¥0.002/千token
qwen-max:      输入¥0.04/千token，输出¥0.12/千token
qwen-long:     输入¥0.0005/千token，输出¥0.002/千token
gemini-2.0:    免费
```

**关键实现：**
- Temperature已从全局配置移除，由各调用点显式控制
- 兜底默认温度：0.85
- DashScope使用 `dashscope.Generation.call()` API
- Gemini使用 `google.genai.Client` API
- 选定模型后会进行一次验证调用

### 3.3 core/planner.py - 规划系统

**职责：**
- 大纲生成/选择
- 角色提取/确认
- 世界观生成
- 人物档案生成
- 风格选择
- 任务卡拆分

**内置风格（6种）：**
1. **爽文宗师** - 主角光环、打脸爽、节奏快
2. **悬疑大师** - 埋线索、设谜团、章尾悬念
3. **情感流** - 细腻情感、感情线丰富
4. **热血战斗** - 战斗燃、情义深、热血沸腾
5. **世界构建者** - 宏大世界观、历史感厚重
6. **轻松日常** - 幽默日常、治愈放松

**任务卡机制：**
- 初始拆分50章（可配置）
- 若不足时调用 `extend_tasks()` 动态拆分
- 预设情绪标签：爽点/冲突/反转/低谷/铺垫

**章节硬约束（必须满足）：**
1. 必须围绕plot_goal推进
2. 人物行为与既有性格一致
3. 与既有设定冲突时以既有设定为准
4. 必须有实质推进（信息新增/关系变化/冲突升级三选一）

**章节禁止项：**
1. 禁止无铺垫硬反转收尾（"突然想起""原来这一切"）
2. 禁止连续空泛抒情或重复表达
3. 禁止把对手写成低智工具人

### 3.4 core/writer.py - 写作系统

**流程：**
1. BEAT_PLANNER生成5-7条节拍
2. 根据节拍生成前半段
3. 根据前半段生成后半段
4. 合并为完整章节

**字数策略：**
- 目标字数：配置 `chapter_word_target`（默认3000）
- 最低阈值：目标的85%（如3000则最低2550）
- 补写：不足则补写第一轮，仍不足则补写第二轮
- 不足两轮后则作为draft，不进审稿
- 超出目标的内容不强制截断

**保存信息：**
- `word_target`：目标字数
- `plot_goal`：情节目标
- `emotion_tag`：情绪标签
- `status`：初始为'draft'（待审稿）

**内部质检：**
- 写作后自动调用SELF_CHECK_SYSTEM进行质检
- 检查：目标偏离/设定冲突/OOC/注水/驱动力
- 质检不通过时调用REVISION_SYSTEM自动修订（1轮）

### 3.5 core/reviewer.py - 审稿系统

**三层评分体系（满分100，需≥75通过）：**

| 层级 | 分数 | 检查项 | 通过要求 |
|------|------|--------|---------|
| L1：逻辑与设定 | 0-45 | 人设一致/时间线/硬设定冲突 | ≥30 |
| L2：伏笔与剧情 | 0-25 | 未兑现伏笔/新伏笔/目标推进 | - |
| L3：可读性与节奏 | 0-30 | 注水/冲突推进/驱动力 | - |

**一票否决项（任一命中即不通过）：**
- `setting_conflict`：核心设定冲突
- `timeline_break`：重大时间线矛盾
- `core_ooc`：主角/核心角色严重OOC
- `critical_payoff_missing`：关键伏笔遗忘

**通过条件：**
- veto_items为空
- score_total ≥ 75
- score_l1 ≥ 30

**重写机制：**
- 不通过自动重写，最多3次（配置 `max_retry`）
- 每次更新 `retry_count`
- 超过3次仍未通过则保存为draft，等待手动强制发布
- 重写反馈包含：否决项+失败归因+问题清单

### 3.6 core/memory_manager.py - 数据管理

**功能清单：**
- 世界观保存/读取 + 同步到 `settings.md`
- 人物档案保存/读取 + 同步到 `characters.md`
- 人物状态更新（位置/状态/章节号）
- 人物关系双向更新（A→B自动镜像B→A）
- 伏笔新增/兑现
- 摘要保存/压缩
- 章节保存/摘要回写/重试次数回写

**关键方法：**
```python
save_chapter(chapter_num, content, plot_goal, emotion_tag, word_target=None)
# word_target未传时使用配置默认值

update_character_relationship(name_a, name_b, relationship, chapter_num)
# 自动双向镜像更新
```

### 3.7 core/db.py - 数据库管理

**表结构：**
- `novel_info`：基本信息（id/name/genre/created_at/status）
- `characters`：人物档案（name/role/外貌/性格/秘密/弱点/位置/状态/关系）
- `chapters`：章节（chapter_num/title/emotion_tag/plot_goal/word_target/content/summary/status/retry_count/review_score_*/审稿信息/时间戳）
- `chapter_tasks`：任务卡（chapter_num/plot_goal/emotion_tag/status）
- `world_settings`：世界观
- `foreshadowing`：伏笔

**连接配置：**
- sqlite3.Row工厂（字典访问）
- 繁忙超时5000ms
- WAL模式（提高并发）

**旧库兼容：**
- `migrate_schema()` 自动补列缺失字段
- 保证旧库可直接继续使用

### 3.8 core/exporter.py - 导出系统

**文本清洗：**
- 删除 `【注释】` 形式注释
- 删除Markdown标记 `**`和 `*`
- 删除分隔线 `---`
- 压缩多换行为两换行

**敏感词处理：**
- 从项目根作 `sensitive_words.txt` 加载
- 格式：每行一词，# 开头被忽略
- 导出时按词长替换为等长 `*`
- 文件不存在或为空时不替换

**导出流程：**
- 读取status为'approved'或'force_approved'的章节
- 进行清洗+敏感词替换
- 保存到 `paths.output_dir/{novel_name}/第XXX章.txt`

---

## 4. 配置说明

```yaml
model:
  max_tokens: 4096                 # API最大输出token数

novel:
  chapter_word_target: 3000        # 单章目标字数
  max_retry: 3                     # 审稿失败最大重试次数
  recent_summary_count: 5          # 写作时回顾的历史摘要数
  compress_after_chapters: 20      # 达到该章数时压缩旧摘要
  pre_split_chapters: 50           # 新建小说预拆分任务卡数
  progress_review_window: 10       # 进度展示的评分窗口

paths:
  data_dir: data                   # 小说数据目录
  output_dir: output               # 导出输出目录
```

**已移除的配置项：**
- `model.temperature`：已全部硬编码在调用点

---

## 5. 数据存储结构

```
项目根目录/
├── main.py                        # 主程序
├── config.yaml                    # 配置
├── sensitive_words.txt            # 敏感词库（可空）
├── core/                          # 核心模块
│   ├── api_client.py
│   ├── config_loader.py
│   ├── db.py
│   ├── exporter.py
│   ├── memory_manager.py
│   ├── planner.py
│   ├── reviewer.py
│   └── writer.py
├── data/                          # 小说数据目录
│   └── {novel_name}/
│       ├── novel.db               # SQLite3数据库
│       ├── outline.md             # 大纲副本（可读）
│       ├── settings.md            # 世界观副本（可读）
│       └── characters.md          # 人物档案副本（可读）
└── output/                        # 导出目录
    └── {novel_name}/
        ├── 第001章.txt
        └── ...
```

**当前存在小说数据：**
- `星落余烬`（数据位置：`data/星落余烬/`，导出位置：`output/星落余烬/`）
- `恐怖鬼屋`（数据位置：`data/恐怖鬼屋/`，导出位置：`output/恐怖鬼屋/`）

**关键特性：**
- 每本小说 = 一个目录 + 一个数据库
- 所有内容存储在SQLite3数据库
- `.md`副本自动同步（用于人工查阅）
- 敏感词库当前仅有注释，尚无实际敏感词

---

## 6. 已知限制与风险

### 6.1 旧小说兼容性
- 自动补列迁移已实装，但旧库数据质量存在差异
- 部分旧小说可能缺少任务卡/novel_info/风格设置
- 程序尽量兼容但不保证所有旧库完全干净

### 6.2 API稳定性
- 高度依赖外部模型接口（DashScope/Gemini）
- 网络波动会导致流程直接失败
- 已观察异常：ProxyError、RemoteDisconnected
- 尚无自动降级策略

### 6.3 任务卡依赖
- 继续写作旧小说若无任务卡，自动触发 `extend_tasks()`
- 该步骤较重，更容易暴露网络不稳定

### 6.4 小说生命周期管理
- 支持：新增、继续写作、删除
- 删除是永久删除，无回收站/撤销
- 已审核章节不可编辑

---

## 7. 后续改进建议

**优先级排序：**
1. 把模型列表从硬编码改成可配置
2. 增加API调用的失败处理和回退策略
3. 增加更细粒度的小说管理（归档、重命名、禁用）

---

## 8. 更新日志

**2026-03-31 00:00 - 文档同步**
- 同步项目真实状态，补充实际存在小说：`恐怖鬼屋`
- 将该变更同步至白皮书（AI网文写作系统白皮书v2.0.md）

**2026-03-30 23:00 - 完整同步**
- 系统梳理流程、配置、模型、审稿、写作等全面可能性
- 补充了所有模块的完整细节说明
- 澄清了硬约束/禁止项/评分标准等关键规则
- 补充了数据存储结构和限制风险的具体描述
